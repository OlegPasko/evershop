Спасибо за заказ, <%= @order.name %>!
===============================================
 
Вы заказали в магазине Optilens следующие товары:
<% sum = 0 %>
<% @order.line_items.each do |i| %>
	— <%= i.quantity %> x <%= i.product.title %> (<%= number_to_currency(i.total_price, :unit => "грн.", :separator => ",", :delimiter => "", :format => "%n %u") %>)
	<% sum += i.total_price %>
<% end %>
Итого на <%= number_to_currency(sum, :unit => "грн.", :separator => ",", :delimiter => "", :format => "%n %u") %>.

Мы свяжемся с Вами в ближайшее время для подтверждения заказа.
Вашему заказу присвоен номер <%= @order.id %>.

Интернет-магазин Optilens
http://optilens.com.ua/
