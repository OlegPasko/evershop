Заказ №<%= @order.id %>!
===============================================
 
<%= @order.name %> <%= @order.created_at %> заказал:
<% sum = 0 %>
<% @order.line_items.each do |i| %>
	— <%= i.quantity %> &times; <%= i.product.title %> (<%= number_to_currency(i.total_price, :unit => "грн.", :separator => ",", :delimiter => "", :format => "%n %u") %>)
<% sum += i.total_price %>
<% end %>
Итого на <%= number_to_currency(sum, :unit => "грн.", :separator => ",", :delimiter => "", :format => "%n %u") %>.

<%= @order.email if @order.email %>
<%= @order.address if @order.address %>
Интернет-магазин Teaco, админка:
http://teaco.com.ua/admin/
